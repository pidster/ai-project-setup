# Cursor

Cursor is an AI-focused code editor with rule, command, and agent interaction
surfaces. Its verified adapter capability data lives in
[capabilities.yaml](capabilities.yaml), and its plugin model data lives in
[plugin-model.yaml](plugin-model.yaml).
