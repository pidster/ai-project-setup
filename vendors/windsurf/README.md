# Windsurf

Windsurf is Cognition's AI coding editor with Cascade customization surfaces.
Its verified adapter capability data lives in
[capabilities.yaml](capabilities.yaml), and its plugin model data lives in
[plugin-model.yaml](plugin-model.yaml).
