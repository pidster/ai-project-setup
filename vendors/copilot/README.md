# GitHub Copilot

GitHub Copilot is GitHub's AI coding assistant and coding-agent product family.
Its verified adapter capability data lives in
[capabilities.yaml](capabilities.yaml), and its plugin packaging model lives in
[plugin-model.yaml](plugin-model.yaml).
