# Claude Code

Claude Code is Anthropic's agentic coding tool. Its verified adapter capability
data lives in [capabilities.yaml](capabilities.yaml), and its plugin packaging
model lives in [plugin-model.yaml](plugin-model.yaml).
