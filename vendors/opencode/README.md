# OpenCode

OpenCode is an agentic coding tool with repository rules, skills, commands,
agents, and runtime configuration. Its verified adapter capability data lives in
[capabilities.yaml](capabilities.yaml), and its plugin module model lives in
[plugin-model.yaml](plugin-model.yaml).
