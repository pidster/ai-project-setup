@AGENTS.md

# Claude Instructions

This repository's policy source is vendor-neutral. Do not treat this Claude file
as a separate policy layer.

When working here:

- Preserve the canonical-core and vendor-adapter boundaries described in
  `AGENTS.md`.
- Keep Claude-specific guidance limited to file format, frontmatter, invocation,
  and capability adaptation.
- Raise conflicts between Claude-specific mechanics and canonical project policy
  instead of silently changing the policy.
